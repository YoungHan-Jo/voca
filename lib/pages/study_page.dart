import 'package:flutter/material.dart';

/// 학습 탭 화면 (Placeholder).
class StudyPage extends StatelessWidget {
  const StudyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Study Page'));
  }
}
