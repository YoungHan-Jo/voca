
# 変更されたファイルに対して CSpell, dart format を実行
echo ""
echo "=========================================="
echo "Running checks on changed files..."
echo "=========================================="
echo ""

if ! npx lint-staged; then
  echo ""
  echo "=========================================="
  echo "❌ Pre-commit checks failed. Please fix the issues before committing."
  echo "=========================================="
  exit 1
fi

echo ""
echo "✅ Pre-commit checks passed!"
